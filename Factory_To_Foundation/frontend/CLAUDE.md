# Factory » Foundation — Frontend OS Handoff

**Status as of this document:** All 11 features are live and running through the shared framework — **Analytics, Administration, and Reports shipped this pass, real data end to end, see §6.9; no features remain on `ComingSoonPage`.** Full design token pass complete. Construction, Genealogy, Scheduling, and now **Manufacturing** run on real (not fixture) data. **Digital twin integration (Milestone B): the twin-side subsystem manifest is done and verified — 13 real subsystems discovered, not the 7 previously visible. The FF-side work to consume that manifest generically was specified and green-lit, but Claude Code (and the Claude desktop app) crashed before its completion status could be confirmed — treat that work as unverified, not done, until checked.** **Manufacturing is no longer waiting on a file — the real Garden Lofts geometry (`Garden Lofts Exterior.blend`, a fully decomposed per-level/per-unit model, not a single fused mesh) has been ingested, converted to glTF, and wired into a real 3D viewport + Building→Level→Unit browse tree. A real defect was found and confirmed in the source geometry during ingestion (not corrected, flagged in the UI) — see §6.5.** **The real `command_queue.json` contract has since been investigated directly (Construction_Enterprises, read-only) — exactly 5 commands exist (pause/resume/reset/step/set_speed), no move/tilt/tool-change command exists yet, and a real, confirmed target-naming divergence was found between `cell_manifest.json`'s ids and the twin's actual `resolve_target()` — see §6.6. That finding now grounds Manufacturing's first cross-feature flow: a "simple version" draft instruction generator (illustrative planning only, never executable, never touching `command_queue.json`) that Factory displays — see §6.7.** **Manufacturing's Shop Drawings now render a real dimensioned 2D plan view — real per-unit geometric adjacency investigated in Blender first (Step 0), baked as `PLAN_DIMENSIONS`, and drawn with a real top-down orthographic camera + CAD-style dimension-line overlay; see §6.8.** **Analytics, Administration, and Reports are built — all real data, no fabricated numbers/users/reports, a new shared `SimplePage` shell for the three pages that don't fit the Browse/Viewport/Selected shape; see §6.9.** **Factory's center viewport is now real 3D digital-twin geometry, not abstract 2D blocks — every shape/dimension/live-transform ported directly from the twin's own `pyvista_render.py` reference viewport, confirmed live moving in real time with the actual twin running; old `FactoryCanvas`/`FactoryNode` deleted. See §6.10.** **Factory gained a fourth panel — a real Gantt chart visualizing the same Manufacturing-generated instruction sequence the "Instructions" dropdown already shows, with an explicitly-illustrative per-step duration estimate; `FeaturePage` gained a generic `workspace` override so Factory's own bespoke 4-panel layout didn't require changing the shared `Workspace` component. See §6.11.** This document supersedes the prior handoff — that one predates the manifest architecture entirely and still describes Factory's twin integration as a hardcoded 7-node translation, which has since been identified as the wrong approach and is being replaced.

---

## 1. What Factory » Foundation Is, Right Now

A React 19 / Vite / TypeScript / Tailwind v4 single-page app. One shared application shell (`FeaturePage`) that every feature renders through — a slim command ribbon at the top, a three-panel resizable workspace below (Browse / Viewport / Selected), all styled through one design token system. Eleven features are routed; all eleven now have real UI (Analytics/Administration/Reports shipped this pass, see §6.9) — `ComingSoonPage` remains on disk, unused by any route, for whatever feature is unbuilt next.

```text
Sidebar (collapsible icon rail) ── routes to ──▶ FeaturePage
                                                    ├── CommandRibbon (dropdown menus + page identity)
                                                    └── Workspace (resizable, fully collapsible)
                                                          ├── Left:   Browse panel
                                                          ├── Center: Viewport (camera pan/zoom/fit)
                                                          └── Right:  Selected/Inspector panel
```

---

## 2. Folder Structure

```text
src/
  framework/           — shared, feature-agnostic
    workspace/          Workspace (react-resizable-panels, all 3 panels collapsible)
    viewport/            Viewport, ViewportControls, CameraContext (pan/zoom/fit engine)
                          EntityNode, EntityCanvas (generic node-graph rendering)
    ui/                  PanelCard, FeaturePage, SimplePage (§6.9), CommandRibbon, DropdownMenu,
                          BrowseList (indent AND children — see §3), ToolbarShell(+Input/Select/Button),
                          StatusBadge, PreviewBox, Legend, DetailRow,
                          KpiCard, KpiRow, KpiList, PageHeader, CollapsibleSection
  features/            — business domain, one folder per feature
    genealogy/           graphData.ts, genealogyRegistry.ts (real QR/prefix scheme, ported from CE_Genealogy_AllInOne-5.py)
    factory/             factoryData.ts (fixture — being retired, see §6.3), useTwinState.ts +
                          twinTranslator.ts (live twin bridge client — mid-rewrite, see §6.3)
    robotics/  logistics/  construction/
    manufacturing/  scheduling/  assets/           ← all 8 FeaturePage features built
    analytics/  administration/  reports/           ← built on SimplePage instead, see §6.9
  pages/               — one page per route, composes a feature through FeaturePage
  router/routes.tsx    — single source of truth: path + label + icon + element,
                          drives both <Routes> and the Sidebar nav
  context/
    SelectionContext.tsx — discriminated union, one global selection engine
  layouts/AppLayout.tsx — Sidebar + content, locked to h-screen
  components/layout/Sidebar.tsx — collapsible icon rail (lucide-react)
  index.css            — design tokens (see §5)
```

`@` resolves to `src` (configured in `vite.config.ts` + `tsconfig.app.json`).

**Outside this project entirely:** `twin-bridge/server.mjs`, a sibling directory to `Factory_To_Foundation` (not under `src/`, not part of the Vite build) — a small dependency-free Node script that polls the real digital twin's `state.json` and serves it over HTTP for Factory's live mode. Also outside this project: the twin's own new manifest-export script, inside `Construction_Enterprises` (see §6.3) — do not confuse the two "manifest" concepts; the twin owns and writes `cell_manifest.json`, FF only ever reads it.

---

## 3. The Framework Layer — What Each Piece Owns

Built incrementally, each promoted to `framework/` only once a second (or third) real feature needed the identical thing — never spec'd in the abstract ahead of a real consumer.

- **`Viewport`** (`framework/viewport`) — owns camera state (`{x, y, zoom}`), left-drag pan, cursor-anchored wheel zoom, double-click fit-to-content, auto-fit on mount/bounds-change. Clips its content; the camera moves, nothing ever overflows the browser. `useCamera()` hook for any child to read/drive it.
- **`EntityCanvas` / `EntityNode`** (`framework/viewport`) — the generic "nodes positioned in world-space, connected by optional edges, click to select" renderer. Originally bespoke per-feature (`GraphNode`/`GraphCanvas`, `FactoryNode`/`FactoryCanvas`); promoted to `framework/` once Robotics/Logistics/Construction/Assets/Scheduling all needed the same shape at once. Genealogy still uses its own bespoke node component (`GraphNode`) because it existed before the promotion and has feature-specific visual treatment (tier-colored borders) that doesn't fit the generic component's props — not a gap, a deliberate non-migration. Factory's `FactoryNode`/`FactoryCanvas` (the abstract 2D block equivalent) no longer exist at all — deleted once Factory's center viewport became real 3D digital-twin geometry instead, see §6.10.
- **`Workspace`** (`framework/workspace`) — three-panel resizable layout via `react-resizable-panels`. **All three panels are `collapsible` with `collapsedSize={0}`** — drag any divider fully closed, drag it back open.
- **`PanelCard`** — the one panel shell every Browse/Viewport/Selected panel renders through. Flat (thin border, minimal radius, no shadow) — not a floating SaaS card.
- **`CommandRibbon` + `DropdownMenu`** — replaced the old full-height `PageHeader` entirely. A single 40px bar: dropdown-triggered menus (Metrics, Filters) on the left, page label + avatar on the right. Only one dropdown open at a time. `PageHeader` still exists, still used by `ComingSoonPage` (on disk, unused by any route now — see §6.9).
- **`SimplePage`** (`framework/ui`) — the shell for pages that don't fit `FeaturePage`'s Browse/Viewport/Selected `Workspace` shape: `CommandRibbon` + a plain scrollable content area. Built once Analytics/Administration/Reports all needed the identical shape at once — see §6.9.
- **`BrowseList`** — **two permanent, independent display modes**:
  - `indent` (flat list, manually-specified depth) — for DAG/sequence data where a single-parent tree would misrepresent it. Genealogy stays on this permanently (the real registry data means Framing-Package has 2 material parents, Sub-Assembly has 4).
  - `children` (real nested tree, expand/collapse per node) — for genuine one-parent containment or real groupings. Construction (Project→Building/Floor), Robotics (each ATC→its real `tools[]`), Logistics (by real `zone`), Assets (by real `category`). Factory's own Browse structure is being reworked as part of the manifest-driven rewrite (§6.3) — its prior "Line→Machines via `lineId`" grouping was fixture-only and doesn't correspond to anything in the real twin, which has no line concept at all.
- **`ToolbarShell` / `ToolbarInput` / `ToolbarSelect` / `ToolbarButton`** — shared flat toolbar primitives, all 8 feature toolbars.
- **`StatusBadge`** — one status pill component, `tone: "positive" | "warning" | "critical" | "neutral"`.
- **`PreviewBox`** — the empty "Preview Image" placeholder shared across every inspector panel.
- **`SelectionContext`** — one global selection engine, discriminated union keyed by `feature`, every payload fully typed (no `unknown`, no casting).
- **`router/routes.tsx`** — single array of `{path, label, icon, element}`, consumed by both `<Routes>` and `Sidebar`'s nav list.

---

## 4. The Eight `FeaturePage` (Browse/Viewport/Selected) Features

| Feature | Data source | Notes |
|---|---|---|
| **Genealogy** | `graphData.ts` — real 7-tier hierarchy (Material → **Framing-Package** → Component → Sub-Assembly → Module → Building → Project), rebuilt from `CE_Genealogy_AllInOne-5.py`'s `build_thread()` | `genealogyRegistry.ts` ports the real prefix→tier registry, QR generation, `canBeFinishedProduct`. The real thread is a DAG (Framing-Package has 2 material parents, Sub-Assembly has 4) — why Genealogy stays on `BrowseList`'s `indent` mode permanently. |
| **Factory** | **RESOLVED, confirmed live — see §6.3.** The manifest-driven rewrite this doc previously listed as crash-unverified was checked directly this pass (`useTwinManifest.ts`/`twinTranslator.ts` read on disk, then actually driven in Chrome with the twin bridge running): all 13 real subsystems render, 7 with live status, 6 honest `unknown`/`--`. Not hypothetical — verified, not assumed. | Also gained a third `CommandRibbon` dropdown, "Instructions" — see §6.7. **Center viewport is now real 3D digital-twin geometry** (ported directly from the twin's own `pyvista_render.py`, real live transforms, real status-colored outlines, click-to-select), not abstract 2D blocks — see §6.10. |
| **Robotics** | `roboticsData.ts` — seeded from the real digital twin subsystem model (`CE_Integrated_Cell_V3_0-6.py`): 1 `RailSubsystem` + 4 `ATCSubsystem`, each with real 4-tool loadouts | Structure is real; live values still placeholders. **Deliberately not wired to the twin bridge yet** — waiting on the manifest-driven pattern to land on Factory first, so Robotics doesn't get built against the same hardcoded approach that's currently being replaced. |
| **Logistics** | `logisticsData.ts` — fixture yard/dock/transportation data | Browse grouped by the real `zone` field. |
| **Construction** | `constructionData.ts` — real Construction Enterprises project data: Stonepine Residences, Cedarwood Flats (Buildings A–E), Garden Lofts (single tower, Floors 1–20), Skyline Towers (single 40-floor high-rise) | Real `children`-based Project→Building/Floor tree. Honest placeholders for unknown data (Stonepine floor plans, Buildings D/E progress) instead of fabrication. |
| **Assets** | `assetsData.ts` — fixture equipment/vehicle/tool data | Browse grouped by the real `category` field. |
| **Scheduling** | `scheduleData.ts` — the real 5-type taxonomy: Inbound Material → Material Arrival → Sub-Assembly/Module Production → Storage & Logistics → Construction Schedule | No KPIs. Inspector honestly shows "no live data yet — owned by [X]." `inbound-material` still has no owning feature — open gap. |
| **Manufacturing** | **Real, ingested geometry — see §6.5.** `garden-lofts-exterior.glb` (915→908 real objects, exported from `Garden Lofts Exterior.blend`), consumed via `gardenLoftsModel.ts` (one shared parser for Viewport/Browse/Inspector, reading real object names + glTF `extras`). | Bespoke `react-three-fiber` `GeometryViewport` (Canvas/OrbitControls/click-to-select) replaced the old empty-state viewport. Browse is a real Building→Level→Unit tree, now with an "Objects" / "Shop Drawings" toggle (real per-unit spec sheets, see §6.7); structural/circulation objects (columns, partitions, core walls) exist in the source but are deliberately left out of Browse for now. Inspector shows real per-unit SF/level data or the real building `PROJECT_SPEC`/`LEVEL_TIER_LABELS` metadata, depending on selection. A confirmed real defect in the source unit distribution (Levels 14–20) is flagged in the UI, not silently ingested as correct. Now also generates a "simple version" draft instruction sequence for Factory to display — see §6.6/§6.7. Scheduling deliberately left untouched. |

**Analytics, Administration, and Reports are built too — see §6.9 for the third real page shell (`SimplePage`) and what each of the three shows.** No feature is on `ComingSoonPage` anymore.

---

## 5. Design System

Light theme, orange accent, Revit/Blender's flat/dense/restrained-accent discipline — not a SaaS dashboard. All tokens in `src/index.css` under `:root` (`--ff-chrome-bg`, `--ff-panel-bg`, `--ff-accent` `#d9631e`, muted tier/status palettes, `--ff-radius: 0.3rem`). Every `shadow-sm`/`rounded-xl` card replaced with flat 1px-bordered panels; selection state is a 2px accent outline, not a glow. Unchanged since the last handoff — no updates this pass.

---

## 6. Known Gaps / Explicit Non-Decisions

1. **RESOLVED — Manufacturing has the real `.blend` file, ingested.** See §6.5 for the full ingestion pass: verification findings, the confirmed real unit-distribution defect (flagged, not corrected), and what's built.
2. **Scheduling's "Inbound Material" has no owning feature.** Still unresolved.
3. **Digital twin integration — see §6.3 for full current state.** Twin-side manifest done; FF-side consumption unverified after a crash.
4. **KPI numbers across Factory/Robotics/Logistics/Assets are fixture data** — plausible, not real, consistently disclosed as such.
5. **A ChatGPT-proposed "control room" inspector mockup was discussed but not built** — worth recording which parts of it are real vs. illustrative, since it's easy to conflate the two later:
   - **Real and cheap, not yet added:** `cycles` already exists per-robot in `state.json`, unused in `FactoryPayload`.
   - **Not possible with the current twin at all:** joint/arm angles — the model has no joint data, only a single rail-position DOF per robot.
   - **Requires new data-model concepts that don't exist anywhere yet:** "Current Program," "Current Module," "Current Wall," "Next Task" — same gap as Scheduling's missing production-run concept.
   - **Requires new infrastructure, not just more polling:** a "Robot History" timeline needs an actual event log; `state.json` is a live snapshot only, nothing historical is captured anywhere today.
6. **The "click to jump to that page's schedule" ribbon button** — never built.
7. **RESOLVED — a third `CommandRibbon` menu type.** Factory's "Instructions" dropdown is that moment, earned by a real cross-feature flow rather than built speculatively. `FeaturePage` gained a generic `extraMenus?: RibbonMenu[]` prop rather than a one-off hardcoded "Instructions" slot, since `CommandRibbon` itself already supported an arbitrary menu array — only `FeaturePage`'s prop surface was artificially limited to two named slots. See §6.7.
8. **`npm run dev` is confirmed runnable in the verification environment** — prefer a live check over `tsc -b --force` alone when a change has a runtime surface, established during the original Twin Bridge slice.
9. **Claude Code and the Claude desktop app both crashed** mid-way through the manifest-driven Factory rewrite. Nothing on disk should be damaged by a crash (only in-memory session state is lost), but **the real completion status of that work is unknown and must be verified, not assumed, before anything proceeds.** First steps on resuming: check `git status` if version-controlled, run `tsc -b --force`, and actually read `twinTranslator.ts`/`server.mjs` to see what's really there rather than guessing from the last thing that was asked for.
10. **Garden Lofts' real source geometry has a confirmed unit-distribution defect (Levels 14–20).** Every residential floor 4–20 has an identical 16-unit floor plate stamped onto it, including a corner 3BR unit on floors 14–20 where the confirmed spec (`garden_lofts_params_v2.json`'s `UNIT_DISTRIBUTION.LEVELS_14_TO_20`) says there should be 0 corner 3BR and 4 corner 2BR. This is not corrected in the ingested geometry — it's flagged in Manufacturing's Inspector/Browse instead (a warning banner + ⚠ marker on the affected units). Full details in §6.5. **Whoever owns the Garden Lofts source file should be told** — this is a real design/floor-plan-application bug upstream of FF, not an FF data problem.
11. **Manufacturing's real per-object geometry counts don't reconcile with the project's aggregate `UNIT_PROGRAM` numbers.** The geometry gives 16 dwelling-unit objects × 17 residential floors = 272 units; `garden_lofts_params_v2.json`'s `UNIT_PROGRAM` totals 340. Neither the per-type breakdown lines up either. Left as an honest discrepancy (same posture as gap #12 below), not silently forced to match.
12. **Whether a Manufacturing "unit" and a Scheduling "module" are even the same concept is still an unresolved, open question.** A prior task brief cited a specific modular summary (442 total modules: 400 residential + 28 corridor/stair/elevator + 14 MEP) as the reason not-1:1 reconciliation was already known — **that figure could not be independently verified this pass** (no "Sheet 2" or matching document was found among the accessible Garden Lofts / Construction Enterprises source files search this pass). Treat it as an unverified prior claim, not a confirmed fact, until someone locates the actual source. Either way, Manufacturing was deliberately **not** wired into Scheduling this pass — don't wire it on the assumption either number is settled.
13. **Real `command_queue.json` command vocabulary is confirmed exhaustive: pause/resume/reset/step/set_speed, nothing else.** No move/tilt-raise/roller-roll/gantry-move/tool-change command exists anywhere in the dispatch code, and ATC tool-swap dispatch specifically is confirmed still not built. See §6.6. Any future FF work that implies real subsystem-move commands exist (beyond whole-cell/single-target pause) would be building on a fiction — check §6.6 first.
14. **Confirmed real divergence between `cell_manifest.json` ids and the twin's actual command-target naming, for robots specifically.** 9 of 13 manifest ids (gantry, roller, tilt, 2 rails, 4 ATCs) work unchanged as a `pause` target; the 4 robot ids (`"robots.A1"` etc.) do not — the real target is the bare `"A1"`. See §6.6 for the full investigation and `resolveCommandTarget()` in `context/ManufacturingOutputContext.tsx` for the one place this translation is now centralized. Any other future FF code that needs a real command target must reuse that function, not re-derive the mapping.
15. **Manufacturing's draft instruction generator is explicitly the "simple version."** It produces illustrative, human-readable planning content — never executed, never written to `command_queue.json`, always labeled "Planning Draft" in the UI. Real machine-executable code for anything beyond whole-cell pause/resume/reset/step/set_speed requires genuinely new commands added to the twin's own dispatch code first — separate, real, protected-codebase work with its own design questions, explicitly out of scope for FF to build toward alone. See §6.7.

### 6.3 Digital Twin — Milestone B, manifest-driven architecture (in progress)

**Why the original first slice got replaced:** the initial Twin Bridge slice (documented in the prior handoff) worked, but hardcoded exactly 7 subsystem identities into `twinTranslator.ts` by name. That meant every time the Python twin grew, someone would have to manually go edit the FF frontend to notice — the opposite of what was wanted. The standing requirement going forward: **the OS must reflect the twin's real complexity dynamically, never via FF-side code hardcoded to today's subsystem list.**

**Twin-side manifest — DONE, verified, real:**
- A standalone, manually-run script inside `Construction_Enterprises` (mirrors `validate_contract.py`'s existing manual-run pattern — deliberately **not** wired into `run_twin_headless.py`, since the object graph is fixed/deterministic at `IntegratedCell.__init__` and nothing adds or removes subsystems at runtime today; this would need to become periodic only if the twin ever gains true runtime subsystem addition).
- Writes `cell_manifest.json`: a real, verified list of **13 subsystems** — gantry, roller, tilt, robots A1/A2/B1/B2 (the original 7), **plus 2 real rail subsystems (Pass 1) and 4 real ATCs (Pass 2)** that FF has never had visibility into before this. That gap between "13 real" and "7 previously visible" is the actual proof this was worth doing, not an over-broad reading of an illustrative example.
- Identity (`id`) follows the same dotted-path naming convention click-to-select already uses — one identity scheme, not a second lookup table that can drift.
- Pure additive, read-only — both twin validation gates (physics-equivalence at frame 1395, `validate_contract.py`) still pass unchanged.
- Minor known detail: label casing needed a small special-case for ATC names ("ATC A Far", not a mechanically-titlecased "Atc A Far") — not purely mechanical, worth knowing if another abbreviation-style type is added later.

**Critical real gap the manifest surfaced:** of the 13 real subsystems, **only 7 (gantry, roller, tilt, the 4 robots) have any corresponding fields in `state.json` today.** The 2 rails and 4 ATCs are real, confirmed identities with **zero live data behind them yet** — `state.json`'s schema hasn't grown to cover Pass 1/Pass 2's object model. This is not hypothetical or a future edge case; it's the real, current shape. The FF-side translator must render these 6 as honest placeholders (id + label, all values `--`) — never omitted, never treated as an error, never fabricated.

**FF-side generic consumption — specified, green-lit, execution status UNVERIFIED (crash):**
The plan (written, not confirmed built):
- Bridge server gains a second endpoint, `GET /twin-manifest`, serving `cell_manifest.json` the same tolerant way `/twin-state` already serves `state.json`.
- `twinTranslator.ts` rewritten to read the manifest for real identities, dispatch to a per-`type` mapping function for known types (robot/gantry/roller/tilt), fall back to a generic raw-field render for an unmapped type that still has `state.json` data, and fall back to honest `--` placeholders for a manifest entry with **no** `state.json` data at all (the real rail/ATC case).
- Floor-plan position (never derivable from live state — `robots.*.x` is rail-travel, not screen position) stays hand-placed for known types, generic-grid-fallback for anything new.
- `factoryData.ts` (the old fixture) was slated for deletion once confirmed unused elsewhere.
- **None of this is confirmed done.** The crash happened before completion could be verified. Do not assume any part of this list is actually in the codebase — check first.

**Also explicitly not started:** Robotics being wired to the bridge/manifest (deliberately waiting for the pattern to prove out on Factory first), WebSocket upgrade (still polling), any live KPIs.

### 6.5 Manufacturing — real Garden Lofts geometry ingestion (done, live-verified)

**Where the real source files actually live — outside this project, outside `Construction_Enterprises` too:** `C:\Users\jchap\Desktop\Blender Files\Garden_Lofts\` — `Garden Lofts Exterior.blend` (the real decomposed model, source of truth for this pass), `Garden_Lofts_Structure.blend` (a separate structure-only file, not used this pass), `garden_lofts_params_v2.json` (the real "Sheet 1" project spec/unit-distribution reference), `garden_lofts_params.json` (an earlier version, superseded by v2), `garden_lofts_curtainwall.py` (the massing-generation script, for context only, not run this pass).

**Step 0 verification — done before any ingestion, per standing discipline (§7):**
- Opened `Garden Lofts Exterior.blend` in real headless Blender (5.1.2, confirmed installed at `C:\Program Files\Blender Foundation\Blender 5.1\blender.exe`). Loads clean, 915 objects, no NaN/degenerate mesh dimensions (the one zero-thickness object, `GL_Site_Plane`, is an expected flat ground plane).
- **First render looked wrong** — thin disconnected floor plates, no facade. Root cause: 219 of 915 objects (columns, balcony bands, etc.) were saved with `hide_render = True`, each carrying a custom property `"GL_visibility_state": "hidden for residential plan inspection"` — a leftover state from someone inspecting floor plans in Blender, not an intentional design exclusion. Confirmed with the user, then overridden (render-visibility only, never touching the saved `.blend`) for export.
- Once restored, the render is a real solid 20-story tower: curtain-wall glazing, podium base, visible green terrace bands at four evenly-spaced heights — matching `GL_SkyGarden_4/8/13/17` and `GL_GardenSoil_4/8/13/17` objects in the file. Confirms the claimed L4/L8/L13/L17 terracing is real.
- **The 3BR-on-every-floor question — confirmed real, not a naming artifact.** `garden_lofts_params_v2.json`'s `UNIT_DISTRIBUTION.LEVELS_14_TO_20` explicitly specifies `CORNER_3BR_PER_FLOOR: 0, CORNER_2BR_PER_FLOOR: 4`. The actual geometry has one `GL_L{level}_3BR_C1` mesh object on **every** level 4–20, each with identical footprint dimensions (11.0m × 8.6m) to the correct L4–13 baseline and custom property `unit_type: "3BR"`, correctly positioned at that floor's real elevation. The 2BR count stays flat at 3/floor (`B1`, `B2_E`, `B2_W`) on every floor 4–20 — never becoming 4 on L14–20 as the spec requires. The glTF node extras even carry `"pattern_source_level": 13, "module_strategy": "repeatable typical residential module"` on the L14+ generated objects — the plan generator literally repeated Level 13's floor plate onto L14–20 without applying the L14–20-specific corner-unit swap rule. **Decision (user-confirmed): ingest the real geometry as-is, flag it in the UI** — not corrected in this pass, not silently trusted as spec-correct. See gap #10.
- **A second, unprompted finding during export:** a large box object, `GL_Residential_Core` (50m × 32m × 54.86m, parented to `GardenLofts_Tower`, tagged with the `GL_Glass` window material — reads as a solid dark-navy block, not clear glass), was pulled into the first export by the blanket "restore all 219 hidden objects" fix. Unlike the columns/balconies, this one doesn't match any real structural element (the real per-floor elevator/stair cores are `GL_L##_CORE`, ~24m×10.8m thin plates) and was hidden *more thoroughly* by the file's author — both `hide_viewport` and `hide_render`, not just `hide_render` like the legitimate architecture. Almost certainly leftover massing/reference scaffolding from authoring. **Excluded from the final export** by leaving its `hide_viewport` alone and exporting with `use_visible=True` (Blender's glTF exporter: `use_visible` checks viewport visibility via `hide_viewport`, *not* `hide_render` — this took an operator-property introspection pass to pin down correctly; don't assume the obvious-sounding kwarg name means what it sounds like without checking `bpy.ops.export_scene.gltf.get_rna_type().properties`).

**Metadata added — real, not fabricated:** `PROJECT_SPEC` and `LEVEL_TIER_LABELS` set as Scene custom properties in Blender, sourced from `garden_lofts_params_v2.json` and the file's own `GardenLofts_Tower` object properties (GFA 24,900 m², footprint 2,010 m², unit mix, construction type, structural system, parking, module/structural-grid dimensions — no per-module W×L×H exists anywhere in the source, so that field says so explicitly rather than inventing a number). `PROJECT_SPEC.known_data_issue` carries the L14–20 3BR defect description so it round-trips into the app automatically. Verified present in the **exported `.glb`'s `extras`** by parsing the binary chunk directly (`scenes[0].extras`), not just trusting the Blender source.

**Export:** `frontend/public/models/garden-lofts-exterior.glb` (908 nodes / 906 meshes survive the round-trip from 915 source objects — a couple of non-mesh helper objects didn't carry over, not consequential). Full real hierarchy preserved — every real object name intact — since Manufacturing's Browse tree depends on parsing them, not a single fused mesh.

**New dependencies (versions checked against npm directly, not assumed):** `three@0.185.1`, `@react-three/fiber@9.6.1`, `@react-three/drei@10.7.7` — note `drei` is on a v10 major, not v9 as an earlier task brief guessed.

**What's built:**
- `features/manufacturing/gardenLoftsModel.ts` — the one shared parser. Reads the loaded glTF scene's real object names and `extras`/`userData` (level, unit_type, unit_label — populated by three.js's `GLTFLoader` from the glTF `extras` the Blender exporter wrote) into a typed `ManufacturingTree`. `GeometryViewport`, `ManufacturingBrowse`, and `ManufacturingInspector` all call this one hook (`useGLTF` caches by URL, so it's one parse) — one identity scheme across all three panels, not a parallel lookup table. Deliberately reads through generic glTF `extras`, nothing Blender-specific, so a future IFC-sourced `.glb` populating the same `userData` shape would work unchanged.
- `features/manufacturing/GeometryViewport/` — real bespoke `react-three-fiber` `<Canvas>` + `OrbitControls` + `<Bounds fit clip observe>` auto-framing. Click-to-select resolves the real object name clicked and drives `SelectionContext` the same way Browse does; clicking a non-unit part of the model (curtain wall, columns, site) falls back to selecting the building root rather than doing nothing. A `SelectionOutline` component draws a real `Box3`-derived wireframe around whatever's selected. Replaces the old empty-state `ManufacturingViewport` (deleted).
- `features/manufacturing/ManufacturingBrowse/` — real `BrowseList` `children`-tree, Building → Level → Unit, parsed from real object names/extras. Levels with real Sky Garden terrace geometry get a 🌿 marker; the flagged L14–20 3BR units get a ⚠ marker. Structural/circulation objects (core walls, partitions, columns, balcony bands, rails, slabs) exist in the real source but are deliberately left out of this tree for now — a scoping choice made explicit in code comments, not missing data.
- `features/manufacturing/ManufacturingInspector/` — branches on `payload.kind`: a selected unit shows real type/subtype/level/SF (confirmed subtype table: S1 410 / S2 420 / A1 640 / A2 650 / B1 1,020 / B2 1,050 / C1 1,550 SF) plus the contradiction warning when flagged; the building root shows the real `PROJECT_SPEC` — genuinely the building-level "shop drawing" view, distinct from a unit's detail.
- `context/SelectionContext.tsx` — `ManufacturingPayload` is now a real discriminated union (`{kind: "unit", ...}` | `{kind: "building", ...}`) instead of the old flat `{name, sourceFile}` placeholder shape.
- `ManufacturingToolbar` — "Import .blend File" button now explicitly disabled with a tooltip explaining conversion is a manual offline Blender step, not something the button does; status text names the real ingested file instead of "No file imported."
- Scheduling — **untouched**, per explicit instruction. See gap #12.

**Live-verified this pass:** `tsc -b --force` clean. Then actually driven in Chrome (not just typechecked) — real tower renders with visible terracing at L4/8/13/17, orbit/pan/zoom works, Browse tree expand/select works, clicking "Garden Lofts" shows the real `PROJECT_SPEC` with the data-issue banner, clicking the flagged `GL_L14_3BR_C1` unit shows the correct warning + 1,550 SF + Level 14, clicking a non-unit part of the 3D model correctly falls back to the building selection, no console errors throughout. Re-verified again after the `GL_Residential_Core` exclusion fix (hard-reload, confirmed the block is gone and the real architecture is still present).

### 6.6 The real `command_queue.json` contract (investigated, read-only, Construction_Enterprises)

Run as its own investigation pass before any FF-side instruction-shape work, per standing discipline (§7) — the same "investigate before building" model as the original Twin Bridge slice. Read `Construction_Enterprises/CLAUDE.md` first, then verified everything below directly against the real dispatch code (`CE_Integrated_Cell_V3_0-6.py`'s `advance()`/HOOK A, `resolve_target()`, `IntegratedCell.__init__`), `validate_contract.py`, and `export_cell_manifest.py`. No files touched, no do-not-modify classes approached.

**The full real command vocabulary — confirmed exhaustive:** `pause`, `resume`, `reset`, `step`, `set_speed`. Nothing else. The dispatch is a single `if/elif` chain in `advance()`; no other place in the codebase reads `command_queue.json`. No gantry-move/tilt-raise/roller-roll/tool-change command exists anywhere. ATC tool-swap dispatch is confirmed still not built (matches `Construction_Enterprises/CLAUDE.md`'s own "NEXT" note) — the Pass 2 object model exists, but nothing in `advance()` ever changes `mounted_tool` after startup.

**Shape per command:** file is `{"command": <string|null>, "params": {...}}`. Only `pause` reads `params.target` (optional, defaults `"all"`); `resume`/`reset`/`step` read no params at all (even a `target` present on `resume` is ignored — Option A, locked); `set_speed` reads `params.speed` (float, clamped `[0.5, 10.0]`). `step` forces exactly one `cell.step()` on the next `advance()` call *before* the pause check, so it fires even while paused.

**Target naming — confirmed NOT the same dotted-path scheme as `cell_manifest.json`, for robots specifically.** `resolve_target(name, cell)` is a flat, single-level `getattr(cell, name, None)` — no dot-splitting. `IntegratedCell.__init__` sets flat attributes for everything (`self.gantry`, `self.rail_A`, `self.atc_A_near`, `self.A1`...`self.B2`) — `self.robots` is a plain list `[A1, A2, B1, B2]`, not a namespace. 9 of 13 manifest ids (gantry, roller, tilt, 2 rails, 4 ATCs) are flat and match `resolve_target` unchanged. The 4 robot ids in `cell_manifest.json` are dotted (`"robots.A1"`) — confirmed via `export_cell_manifest.py`'s own docstring to be borrowed from `pyvista_render.py`'s click-to-select part-labeling convention, a *different* scheme, not from `resolve_target`. `getattr(cell, "robots.A1", None)` does not resolve; the real target is the bare `"A1"`. A related footgun: `getattr(cell, "robots", None)` "succeeds" (the list is truthy) — a literal `"robots"` target would silently resolve to something meaningless rather than erroring.

**No formal schema exists.** No JSON Schema/dataclass/Pydantic model for commands anywhere in `Chappell_Robotics/`. `validate_contract.py` is a behavioral test harness, not a schema — it only exercises `pause` (targeted-success + unknown-target-error paths) and `resume`; `reset`/`step`/`set_speed` are untested. The dispatch if/elif chain itself is the only source of truth. One more asymmetry: an unknown *target* on `pause` populates `_last_error`; an unknown *command* does not — silently dropped, no error surfaced anywhere.

### 6.7 Manufacturing → Factory: the first cross-feature data flow (done, live-verified)

**The "simple version," explicitly.** A derived, human-readable planned sequence — never executable, never written to `command_queue.json`. Real machine-executable code for anything beyond §6.6's five commands requires new commands added to the twin's own dispatch code first — separate, future, protected-codebase work, not something FF builds toward alone. Run only after §6.6 landed, since the instruction shape depends on the real command vocabulary found there, not a guessed one.

**`context/ManufacturingOutputContext.tsx` (NEW)** — same pattern as `SelectionContext`: context + provider + hook, in-memory only, not persisted (regenerating from the real ingested geometry + live manifest is cheap). `InstructionStep` carries **two** target fields, not one — `targetSubsystemId` (the real manifest id, e.g. `"robots.A1"`, kept for display/cross-referencing since that's what the manifest and Factory's UI already use) and `realCommandTarget` (the actual `resolve_target()`-compatible name, e.g. `"A1"`). `resolveCommandTarget()` is the one, explicitly-documented translation function (strip a `"robots."` prefix, otherwise pass through) — every future consumer that needs a real target must call this, not re-derive the mapping, so §6.6's confirmed-real bug can't quietly reappear. `action` is labeled illustrative-only in its own doc comment. `App.tsx` wraps the tree in `<ManufacturingOutputProvider>` alongside `<SelectionProvider>`.

**Generation (`features/manufacturing/instructionGeneration.ts`, NEW):** crosses a fabrication-relevant slice of the real 7-tier genealogy progression (Material→Framing-Package→Component→Sub-Assembly→Module — Building/Project excluded, portfolio tiers not cell operations) with the twin's real master-phase sequence (`RAIL_WORKING → ROLLER_TRANSFER → TILT_RAISING → GANTRY_PICKUP → RETURNING/COMPLETE`, confirmed via `IntegratedCell.step()`), producing 4 steps: robot+rail framing/fastening, roller transfer, tilt reorientation, gantry pickup/place. Every `targetSubsystemId`/`realCommandTarget` comes from a real `TwinManifest` lookup (`useTwinManifest`, imported directly from `features/factory` — a second real consumer, not new logic); a missing manifest id fails generation loudly with a reason string instead of inventing an id. Side (A/B, i.e. which rail/robot/ATC) is picked deterministically from a stable hash of the source object id — not random, regenerating the same target yields the same sequence.

**Triggered from `ManufacturingToolbar`** — "Generate Shop Drawings & Instructions" button, scoped to the currently-selected unit if one exists, or a building-level "representative unit" run otherwise (explicitly labeled as representative, not claimed to cover all real units at once). Shows an inline status message either way (success with step count, or "twin manifest not reachable" if the bridge/manifest isn't up).

**Shop drawings, inside Browse Geometry per the user's literal framing:** `ManufacturingBrowse` gained an "Objects" / "Shop Drawings" toggle in `PanelCard`'s header `toolbar` slot (first real use of that slot). Shop Drawings mode shows the same real Building→Level→Unit shape but as one spec-sheet per unit; clicking a sheet is a self-contained local master/detail view (not routed through `SelectionContext`) showing real unit type/subtype/SF/level/object name, the real structural bay grid (9.144m = 30.0' exactly, confirmed), and **balcony depth measured live from the matching `GL_BalconyBand_{level}_*` object's real bounding box** (not a looked-up spec number — none exists in the source data for this). **Real bug found and fixed during live verification:** the first version searched `scene.children` (root-level only) and never found a match, since `GL_BalconyBand_*` objects are parented under `GardenLofts_Tower`, not root siblings — fixed to `scene.traverse()`. Corridor width has no matching named object anywhere in the source geometry (confirmed by search, not assumed absent) — shown as an honest "not confirmed," not invented.

**Factory's "Instructions" dropdown (NEW, third `CommandRibbon` menu — see gap #7):** `FeaturePage` gained a generic `extraMenus?: RibbonMenu[]` prop (not a one-off hardcoded slot) since `CommandRibbon` already supported an arbitrary menu array. `features/factory/FactoryInstructions.tsx` reads `useManufacturingOutput()` read-only, shows a persistent "Planning Draft" disclaimer banner, then each step's action/target/real-target/type — or "No instructions yet — generate them from Manufacturing" if nothing has been generated. In-memory-only was confirmed the honest choice, not just a spec line: a full browser navigation (address-bar style, not an in-app link click) legitimately clears the context and correctly shows the empty state — verified both ways, not assumed.

**Live-verified this pass:** `tsc -b --force` clean (including after the balcony-depth fix). Then driven end-to-end in Chrome via real in-app sidebar navigation (not address-bar reloads, which would have hidden a state-loss bug behind an expected-empty-state look-alike): selected a real unit in Manufacturing, generated a 4-step instruction set, navigated to Factory via the sidebar, confirmed the same 4 real steps appear with correct `targetSubsystemId`/`realCommandTarget` divergence visible for the robot step (`robots.B1` → real target `B1`) and identical for the roller/tilt/gantry steps, then separately verified the building-level "representative unit" path and the honest empty state. No console errors throughout either page.

### 6.8 Manufacturing — real 2D dimensioned plan-view drawings (done, live-verified)

**Step 0 (Blender, headless `bpy`), run and reported before any baking:** investigated real geometric adjacency for `GL_L07_1BR_A1_E` and generalized across unit types. Confirmed method: a unit's **East/West party walls** resolve by matching against `*Partition_*` objects whose own bounding box is vertically-oriented (`dims.x < dims.y`, i.e. the object's thin axis is its wall thickness) within `GAP_TOL = 0.05m` gap and `OVERLAP_FRAC = 0.85` depth overlap — real matches clustered at 0.0–0.04m gap, false positives started at 0.14m+, a clean separation. Confirmed **no North/South wall object exists for any tested unit at all** — `GL_Slab_##` floor plates were the only "close" candidates and were confirmed false positives, not a missed match. Confirmed on a corner unit (`GL_L07_1BR_A2_NE`) that the method can honestly fail: candidates existed but at 0.34–0.44m gap, well outside tolerance — correctly left unresolved rather than forced. This is why `overallDepth_m` is always the unit's own real Y-extent (never a second wall reference) and why corner/`A2`-family units routinely carry empty `boundingElements`.

**Baked as `PLAN_DIMENSIONS`,** a real per-unit custom property (`outline`, `overallWidth_m`, `overallDepth_m`, `boundingElements: {id, side, distance_m}[]`, `unresolvedSides: string[]`) on all 272 `GL_L{level}_{type}_*` unit objects, exported through the same `.blend → .glb` pipeline as `PROJECT_SPEC`/`LEVEL_TIER_LABELS` and verified round-tripped by direct binary glb parsing. **Real finding, not a bug:** 238 of 272 units (87.5%) have ≥1 unresolved side; every `1BR_A2_*` and `STUDIO_S2_1` unit has **both** sides unresolved, consistently across all 17 floors — genuinely sparser data than a naive "every unit has two party walls" assumption would produce, baked and surfaced as-is per explicit user sign-off rather than papered over.

**FF-side, `GeometryViewport.tsx`:** a new `viewMode: "3d" | "plan"` toggle. **"3d" mode gained real per-unit isolation** (not previously built — done here as an explicit prerequisite) via drei's official `useBounds()`/`bounds.refresh(target).fit()` pattern. **"plan" mode is a real top-down orthographic camera** (`THREE.OrthographicCamera`, manually constructed and registered via `useThree().set({camera})`, not `drei`'s `<OrthographicCamera makeDefault>`) framed to the selected unit's live `Box3` — deliberately re-measured from the loaded three.js object rather than reusing baked Blender-space coordinates, sidestepping the Z-up/Y-up axis conversion entirely. A real CAD-style `DimensionOverlay`/`DimensionLine` draws extension lines, a dimension line with end ticks, and a real-value text label for overall width, overall depth, each real confirmed party wall (or "0.00 m (touching)" when the gap is ~0), and an honest "No confirmed adjacent wall (side)" note for each `unresolvedSides` entry — never a fabricated dimension.

**Two real, non-obvious bugs found and fixed live, both worth carrying forward as lessons:**
1. **Plan view rendered nothing despite provably-correct camera math** (position/frustum/projection matrix all verified sane via logging) through four failed iterations of camera-construction approach. **Real root cause:** `<OrbitControls>` was mounted *unconditionally* in the Canvas (only its `enableRotate` prop toggled) and was silently overriding the manually-swapped default camera every frame before a frame could ever paint. Fix: mount `<OrbitControls>` conditionally on `viewMode === "3d"`, matching how `Bounds`/the perspective camera were already conditional. **Lesson: any always-mounted camera-adjacent drei helper is a suspect the moment a second, manually-managed camera is introduced — check what's unconditionally mounted before re-deriving camera math from scratch.**
2. **Dimension labels on the west edge visually overlapped** even after their world-space offset was increased — because `Text`'s rotation was a fixed `[-Math.PI/2, 0, 0]` regardless of the dimension line's real orientation, so a *vertical* (west/east-edge) label still read horizontally along world-X, and its wide glyph run reached across into its horizontal-edge neighbor's lane regardless of spacing. Fix: text now yaws to match each dimension line's own `along` vector (`yaw = atan2(-along.z, along.x)`, applied as a nested group's local-Z rotation composing with the flat-lay rotation as a true vertical-axis yaw) — every label now reads along its own dimension line, CAD-convention-correct, and the overlap is structurally impossible rather than spacing-tuned away.

**Explicitly not built, per task scope:** no title block/sheet layout, no print/PDF export, no full-floor composite drawing — this is a single-unit dimensioned plan view only.

**Live-verified this pass:** `tsc -b --force` clean, zero leftover debug logging. Tested two structurally different real units end-to-end from Shop Drawings sheets (auto-defaulting into plan view, per §6.5's existing `preferredView` wiring): `GL_L04_1BR_A1_E` (interior unit, both party walls resolved — 0.04m west gap, 0.00m/touching east) and `GL_L04_1BR_A2_NE` (corner unit, both sides genuinely unresolved — rendered as two clean "No confirmed adjacent wall" notes, no fabricated dimension). Toggled 3d→plan→3d cleanly with no broken camera state in either direction. Sanity-checked against the confirmed Sheet-1 subtype SF table: A1's real 7.20m × 8.60m ≈ 666 SF vs. table's 640 SF, A2's real 8.80m × 6.60m ≈ 625 SF vs. table's 650 SF — both plausible, neither implies a runaway/mis-scaled dimension.

---

### 6.9 Analytics, Administration, Reports — real data, one pass (done, live-verified)

**New shared shell — `framework/ui/SimplePage.tsx`.** None of the three fit `FeaturePage`'s Browse/Viewport/Selected `Workspace` shape (the original spec never gave them a viewport at all — dashboard/settings/document-library shapes, not spatial object browsing). Since all three needed the identical "ribbon + free content area" shape at once, that's a real three-consumer signal, built once rather than three one-off layouts: `CommandRibbon` (page identity, optional extra dropdowns via the same `RibbonMenu` mechanism `FeaturePage` already uses) + a plain scrollable content area. `FeaturePage` itself is untouched — `SimplePage` is a separate, parallel shell, not a replacement.

**Analytics — real cross-feature dashboard, `features/analytics/AnalyticsDashboard.tsx`.** Five widgets, each reading a real existing data source directly, no new metrics-computation layer invented:
- **Factory:** `useTwinManifest()`/`useTwinState()` + `translateManifest()` (all three already built for Factory itself — a new real consumer, not new logic). Shows real subsystem count, real running/idle/unknown breakdown. Genuinely live: confirmed by killing and restarting the twin-bridge process mid-session — the widget flips between a real "Live Twin Data" badge with real counts and an honest "Twin Offline — start `twin-bridge/server.mjs`..." explanation with zero fabricated numbers, matching Factory's own page exactly.
- **Genealogy:** real per-tier node counts and finished-product-capable count, computed from `graphData.ts`'s real 13-node thread + `genealogyRegistry.ts`'s `canBeFinishedProduct` — not hardcoded.
- **Manufacturing:** real ingested unit count (272) and real flagged-unit count (7 — one corner 3BR per floor across the 7 real defect floors, L14–20), read live via `useGardenLoftsTree()`. Wrapped in its own `<Suspense>` boundary (the glTF load suspends) so it doesn't block the other four widgets from rendering immediately.
- **Construction:** real project count (4) and each project's real child count/type straight from `constructionData.ts`'s actual tree shape (e.g. "Cedarwood Flats — 5 Buildings", "Garden Lofts — 20 Floors") — no fabricated per-project unit totals, no attempt to reconcile against Manufacturing's 272-vs-340 discrepancy (gap #11, explicitly not this task's job).
- **Scheduling:** the 5 real stages, 4 real wires, and how many stages have no live data yet — structural facts, explicitly labeled "Structural Facts — Not Live," not live production data.

Every widget carries a `StatusBadge` disclosing what kind of real it is (`Live Twin Data`/`Twin Offline`, `Real Static Data`, `Real Ingested Geometry`, `Structural Facts — Not Live`). Logistics/Assets/Robotics are deliberately not built as widgets (their KPI values are fixture, per gap #4/#5) — a footer note says so explicitly rather than silently omitting them or fabricating placeholder cards.

**Reports — real document library, `features/reports/ReportsLibrary.tsx`.** Reads the same in-memory `useManufacturingOutput()` context Factory's "Instructions" ribbon menu already reads — not a second store. Genuinely empty most of the time (confirmed live): a fresh session shows an honest "No reports generated yet — Manufacturing's Shop Drawings and Generate Shop Drawings & Instructions actions populate this list" explanation, not a generic empty state. After generating a real instruction set from Manufacturing (via real in-app sidebar navigation, not an address-bar reload — same non-persisted-state discipline as §6.7's original cross-feature verification), Reports shows the real 4-step sequence live, same "Planning Draft" disclaimer, same real `targetSubsystemId`/`realCommandTarget` divergence for the robot step.

**Administration — real data-provenance table, not fabricated user management.** The original spec (Users/Roles/Permissions/Security/Audit Logs) assumes a real auth/user system this app doesn't have — not built. Instead, `features/administration/dataProvenance.ts` is one small, explicitly-maintained config table stating each of the 8 `FeaturePage` features' real current status (`real-live` / `real-static` / `real-structure-fixture-values` / `fixture`), a real summary sentence, and its real source file — this operationalizes what CLAUDE.md's §4 table and §6 gap list already say in prose into real, live UI. `DataProvenanceTable.tsx` renders it as a real HTML table with a `StatusBadge` per row. This is the one place that needs updating when a feature's real status changes later (e.g. when Robotics gets wired to the twin bridge).

**Routing:** `router/routes.tsx` now points `/analytics`, `/administration`, `/reports` at the three new pages. `ComingSoonPage.tsx` itself is untouched on disk, just no longer referenced by any route.

**Live-verified this pass:** `tsc -b --force` clean. Then driven live in Chrome: Analytics loaded with the twin bridge up (real 13/7/4/3/6 subsystem breakdown, real 13-node/2-finished-product genealogy counts, real 272/7 Manufacturing counts, real 4-project Construction breakdown, real 5/4/1 Scheduling counts) and with the bridge killed mid-session (Factory widget alone flipped to the honest offline explanation, nothing else changed) — confirming the widget is genuinely live, not a static snapshot taken once. Reports confirmed empty-by-default, then confirmed showing a real freshly-generated 4-step sequence after using Manufacturing's real "Generate Shop Drawings & Instructions" action and navigating via the sidebar. Administration's table confirmed rendering all 8 real rows with accurate status badges. One stale console error (`camRef is not defined`, referencing a bug from a much earlier debugging session in `GeometryViewport.tsx`) appeared in the browser extension's console buffer throughout — confirmed not real: the file has no such reference on disk, and Manufacturing's 3D view rendered and behaved correctly live in the same tab. Treated as leftover extension-side log-buffer noise, not a regression.

### 6.10 Factory — real 3D digital-twin geometry, replacing the abstract 2D blocks (done, live-verified)

**Step 0 (separate investigation pass, reported before any building):** read `Construction_Enterprises/CLAUDE.md` then `Chappell_Robotics/pyvista_render.py` directly. Confirmed, uniformly across every subsystem (gantry, roller, tilt, rails, robots, ATCs): geometry is built entirely from PyVista's own primitive generators (box/quad/line/sphere/cylinder), never loaded from external CAD/mesh files, using real named dimension constants from `CE_Integrated_Cell_V3_0-6.py` (`FIXED_W=6.6`, `ROLLER_R=0.05`, `BRIDGE_BEAM_Z=7.8`, `RAIL_A_Y=-1.55`, etc. — real meters, spot-checked against the source, not placeholders). Live transforms are real trig/translation, mostly living in reusable `TiltTable`/`RollerTable` class methods (`leaf_corners()`, `cyl_tip()`, `pin_pos()`, `panel_visible`) that `pyvista_render.py` itself reuses rather than re-deriving. **The one real caveat, confirmed directly:** the robot arm's visually richest detail (elbow/tip pose, 7 labeled parts) comes from an inline 3-branch cosmetic pose selector keyed on `robot.working`/`changing_tool`, not real per-joint telemetry — and this is true even in the twin's own reference viewport, not a gap introduced by porting it. Full findings reported and user-confirmed before any code was written.

**Real geometry construction — `features/factory/twinGeometryConstants.ts` (NEW):** every real named dimension/color constant from the Step 0 investigation, ported directly (not re-derived), plus the one `toThree(x,y,z) => [x,z,y]` conversion point (the twin's Python code is Z-up like PyVista; three.js is Y-up) — every consumer calls this instead of reordering coordinates by hand, so the axis mapping can't drift between subsystems.

**`features/factory/FactoryGeometryViewport/` (NEW) — replaces `FactoryLayout`/`FactoryCanvas`/`FactoryNode` entirely (deleted, confirmed unused first):** same bespoke `react-three-fiber` pattern already proven in Manufacturing's `GeometryViewport` (`<Canvas>` + `OrbitControls` + click-to-select driving `SelectionContext`), reused rather than reinvented. Real per-subsystem geometry (gantry: columns/beam/hoist/hook; roller: frame/rollers/live panel; tilt: base/leaf/hydraulics/pins/lift-panel; rails; ATC racks; robots) driven live by the exact same `useTwinManifest`/`useTwinState`/`translateManifest` Factory's Browse/Inspector already used — a new real consumer, not new logic. **Status legibility (carried forward from the old 2D `FactoryNode`'s colored left-edge strip):** rather than tinting each part's real material color (which would throw away real per-part color information ported faithfully from the twin), each subsystem gets a real-status-colored bounding-box wireframe outline (reusing the same `Box3` technique as Manufacturing's `SelectionOutline`), always visible, plus a thicker accent-colored outline for whichever subsystem is currently selected. **Rails/ATCs get real, correctly-positioned 3D geometry now, but still honestly show `unknown` status** — those are two independently-true facts (real geometry existing vs. real live data existing), not conflated. **The robot illustrative-pose disclosure is a real, unmissable in-scene `<Text>` label** (not a tooltip), placed on the open floor area away from the machinery cluster, stating plainly that the pose is a 3-pose cosmetic branch, not real joint telemetry — matching the same honesty standard as Manufacturing's "Planning Draft" disclosure.

**Two real, non-obvious bugs found and fixed live, both worth carrying forward:**
1. **`Bounds`/`useBounds()` — both the declarative `fit` prop and the imperative `refresh().fit()` call — were tried first and confirmed live to never actually reposition the camera** (it stayed pinned at react-three-fiber's raw default `[0,0,5]`, confirmed by direct `camera.position`/scene `Box3` inspection via injected debug logging, not assumption). Near/far planes updated correctly (`clip` working), only actual repositioning silently failed. Root cause not fully pinned down (almost certainly `OrbitControls`, mounted with its own default `target=[0,0,0]`, fighting the fit every frame — the same class of bug as Manufacturing's plan-view camera work, just manifesting through `Bounds` instead of a manual camera swap this time). **Fix:** dropped `Bounds`/`useBounds()` entirely; used the same manual ref-based `InitialPerspectiveCamera`-style technique already proven in Manufacturing, plus explicitly passing `OrbitControls` a non-default `target` matching the real scene center (Manufacturing's own content happened to already be centered near the origin, which is why that pattern never needed an explicit target before). **Lesson: `Bounds`' declarative/imperative fit mechanisms are not reliable on their own in this codebase whenever `OrbitControls` is also present — prefer the manual camera + explicit `OrbitControls target` pattern from the start rather than debugging `Bounds` cooperation.**
2. **A real, separate "blank viewport" red herring: react-three-fiber's `<Canvas>` starts at its raw default size (`300×150`) and resizes asynchronously once its `ResizeObserver` reports the real container size** — a live screenshot taken immediately after navigation can genuinely catch the canvas mid-resize (confirmed directly: `canvas.getBoundingClientRect()` read `300×150` on one check and the real `830×586` moments later with no code change in between). This cost significant debugging time chasing camera/`Bounds` theories before the actual canvas-size timing was isolated via direct DOM inspection. **Lesson: when a freshly-navigated R3F view looks blank, check the canvas's actual measured size before assuming a camera/geometry/material bug** — a `getBoundingClientRect()` check take priority over screenshot timing.

**Live-verified this pass:** `tsc -b --force` clean, zero debug logging left in the file. Restarted the real twin (`run_twin_headless.py`) fresh and confirmed live in Chrome: real geometry renders for every subsystem at real proportions (fixture table, striped roller bed, gantry A-frame, robot arms all visually correct relative to each other); **watched the gantry visibly move between two screenshots taken 3 seconds apart, matching a real `bridge_x` change confirmed independently by reading `state.json` directly** (not just "it rendered once correctly") — the one acceptance criterion the task called out explicitly as not sufficient to skip. Click-to-select confirmed twice: clicking the tilt table in 3D resolved to the real `tilt` manifest id and showed real Inspector data (`Idle`, live state `FLAT`); selecting `Robot A1` from the Browse list correctly highlighted the same real robot in the 3D view with an accent outline. Status-colored outlines confirmed visible (green on the running gantry/robot, amber on idle tilt). The illustrative-pose disclosure text confirmed legible in-scene after repositioning it off the floor plane (it was originally placed slightly below floor level, real z=-0.35, and effectively invisible — moved to real z=0.02, clear of the machinery cluster). `FactoryBrowse`/`FactoryInspector` confirmed unaffected — same real manifest/state data flow as before, no changes needed.

### 6.11 Factory — Gantt chart panel, real sequence + illustrative timing (done, live-verified)

**Same real data, a second real view.** Reads the exact same `useManufacturingOutput()` context Factory's "Instructions" `CommandRibbon` dropdown (`FactoryInstructions.tsx`) already reads — not a second store, both stay, this is purely additive. `InstructionStep` gained `estimatedDurationSec` (30s robot framing/fastening, 15s roller transfer, 20s tilt reorientation, 15s gantry pickup/place) — explicitly documented as an illustrative round estimate, not measured or derived from any real timing data, since nothing in the twin exposes real per-step duration. Set once in `instructionGeneration.ts` alongside the existing step generation, same honesty standard as the `action` field's existing illustrative-only doc comment.

**Layout — bespoke to Factory, not a change to the shared `Workspace`:** `features/factory/FactoryWorkspace.tsx` (NEW) nests a second, vertically-oriented `react-resizable-panels` `Group` inside the right-hand `Panel` of the existing horizontal Browse/Main split — a `Panel` containing its own differently-oriented `Group` is already-supported nesting, no library workaround needed. Shape: `Browse | (Viewport + Selected Machine) over Gantt Chart`. Every panel keeps the same `collapsible`/`collapsedSize={0}` behavior as the shared `Workspace`. `FeaturePage` gained a small additive `workspace?: ReactNode` override prop (same extension-point pattern as `extraMenus`) — when provided, it renders instead of the default `left`/`center`/`right` three-panel composition, so `framework/workspace/Workspace.tsx` itself needed zero changes. Only promote this 4-panel shape to `framework/` if a second real feature ever needs it too.

**`features/factory/FactoryGanttChart.tsx` (NEW):** real time ruler (illustrative — the durations underneath it are illustrative, so the ruler doesn't claim to be a real clock), one parent bar spanning the real generated sequence's total duration (sum of real step `estimatedDurationSec` values), expandable/collapsible into one real child bar per `InstructionStep`, each positioned at its real cumulative start offset with width proportional to its own real duration — not equal-width placeholder bars. Each bar labeled with real `action`/`targetSubsystemId`/`realCommandTarget`. Same "Planning Draft" disclosure wording `FactoryInstructions.tsx` already uses (not a second, differently-worded disclaimer for the same underlying fact), plus one added sentence noting bar widths are illustrative estimates. Same exact empty-state wording as `FactoryInstructions.tsx`: "No instructions yet — generate them from Manufacturing."

**What stayed untouched, confirmed:** `FactoryBrowse`, `FactoryGeometryViewport`, `FactoryInspector`, and the existing "Instructions" dropdown — all purely additive, same real manifest/state/instruction data flow as before.

**Live-verified this pass:** `tsc -b --force` clean. Confirmed the honest empty state before generating anything. Generated a real 4-step instruction set from Manufacturing, navigated to Factory via the sidebar (real in-app navigation, not an address-bar reload — same non-persisted-state discipline as §6.7), and confirmed the Gantt chart renders real proportional bars matching the real math exactly (Full Sequence 80s = 30+15+20+15; Step 2's bar starts at real x=30s, Step 3's at x=45s, etc. — cumulative offsets correct, not eyeballed). Confirmed the new horizontal divider drag-resizes the Viewport+Inspector/Gantt split, drags fully closed to `collapsedSize=0`, and drags back open with the chart's real data still intact — same behavior class as the other three panels. Confirmed the parent row's expand/collapse toggle hides/shows the real child step rows. No console errors throughout.

## 7. Standing Engineering Discipline

- **Promote to `framework/` only on a second or third real consumer**, never speculatively.
- **Don't fabricate data where real data exists or is knowable** — extends to derived fields (never fabricate a "down"/fault state the twin doesn't expose) and to entire objects (the 6 rail/ATC nodes with no live data yet are still real and still shown, just honestly empty).
- **`tsc -b --force` after every change; prefer an actual live check (`npm run dev`) over `tsc` alone** whenever the change has a runtime surface — confirmed possible in this environment since the original Twin Bridge slice.
- **Sequence, don't stack.** Every new idea that's arrived mid-task all session has been explicitly deferred to its own milestone rather than absorbed into whatever was in flight.
- **Investigate before building, report real findings before writing code** — for the twin's own codebase and for any FF work depending on an external real data source. This is what caught the object-model mismatch in the first Twin Bridge slice, and what caught the "13 not 7" gap this pass.
- **When a real data source doesn't map cleanly onto an existing shape, use the real shape — don't force it.** Confirmed repeatedly: Construction's real projects, Factory's real subsystem count (twice now — 7 was already a correction, 13 corrected it further), Genealogy's real DAG structure.
- **Never hardcode an external system's current shape into FF when that system can grow.** The specific, hard-learned version of the above principle from this pass: a translator that hardcodes "these are the N things that exist" is already wrong the moment the source system changes, even if it's correct today. Prefer consuming a self-describing manifest/schema over enumerating identities by hand, whenever the source system can plausibly grow.
- **After any crash or interruption, verify real on-disk state before resuming — never assume completion from the last thing that was asked for.** New this pass, for good reason: an agent crashing mid-task doesn't damage saved files, but it does make "what actually got finished" genuinely unknown until checked.
- **A visibility/inclusion toggle left in a source file by a prior authoring session is data, not noise — read why it's set before overriding it, and don't treat all instances of it as the same thing.** From the Manufacturing ingestion pass (§6.5): 219 objects shared the identical `"hidden for residential plan inspection"` tag, but they weren't all the same kind of hidden — the real architecture (columns, balconies) was only `hide_render`-hidden, while one leftover massing block (`GL_Residential_Core`) was hidden more thoroughly (`hide_viewport` too). That distinction was the real signal for what to keep vs. exclude, and it only surfaced by checking each flag specifically rather than restoring everything with one blanket toggle. The same pass also found that an export operator's boolean kwarg (`use_visible`) didn't mean what its name suggested at a glance — verify a tool's actual flag semantics (e.g. via `get_rna_type().properties`) rather than assuming from the name.
- **Don't carry an unverified claim from a task brief into a permanent doc as if it were confirmed.** The Manufacturing brief asserted a specific number (442 total modules) as a settled fact; it couldn't be located in any accessible source file this pass. It's recorded here as an explicitly unverified prior claim (gap #12), not repeated as fact — the same standard Step 0 already applied to the geometry itself.
- **Two systems that look like they share an identity scheme (matching naming convention, same-looking dotted paths) can still diverge in a way that only shows up by reading the actual consuming code, not the producing side.** `cell_manifest.json`'s ids and the twin's real `resolve_target()` matched for 9 of 13 identities and looked like one scheme — reading only the manifest/exporter side would have missed that the 4 robot ids don't actually resolve. The fix (§6.6/§6.7) was reading the consumer (`resolve_target`) directly rather than trusting that a shared-looking convention was actually shared everywhere it was used.
- **A doc that says a prior pass is "unverified after a crash" can itself go stale** — this pass found `useTwinManifest.ts`/`twinTranslator.ts` fully built and working on disk, contradicting this document's own then-current §4 Factory row. Trust `git status`/reading the files/`tsc -b --force` over a doc's last-known status line whenever they might have diverged, and update the doc immediately once checked rather than letting the discrepancy persist.
- **When testing a cross-feature flow backed by in-memory (non-persisted) state, navigate the way a real user would (in-app links), not via a full address-bar reload** — a hard reload legitimately clears in-memory context and will make a real bug (state not actually flowing between features) look identical to the correct empty-state UI. Caught by testing both navigation styles deliberately, not by accident.
- **A freshly-navigated react-three-fiber `<Canvas>` genuinely can look blank for reasons that have nothing to do with the scene at all** — it starts at a raw default size (`300×150`) and resizes asynchronously once its `ResizeObserver` reports the real container size; a screenshot taken immediately after navigation can catch it mid-resize. From Factory's 3D viewport pass (§6.10): direct `canvas.getBoundingClientRect()` inspection resolved this in one check after camera/`Bounds` theories had already burned real debugging time. Check the canvas's actual measured size before assuming a camera/geometry/material bug on a "blank on load" report.
- **`Bounds`/`useBounds()`'s fit mechanisms (both the declarative `fit` prop and the imperative `refresh().fit()` call) are not reliable in this codebase whenever `OrbitControls` is also mounted** — confirmed twice now, independently, in two different R3F viewports (Manufacturing's plan-view camera swap, and Factory's `Bounds`-based framing in §6.10.) Prefer the manual technique proven in both places instead: a ref-based initial camera position + an explicit non-default `OrbitControls` `target` matching the real scene center, set once. Don't spend time trying to make `Bounds` cooperate with `OrbitControls` — reach for the manual pattern directly.
- **A shared page shell can gain a generic override extension point without changing the component it wraps.** `FeaturePage` gained an optional `workspace?: ReactNode` prop for Factory's bespoke 4-panel Gantt layout (§6.11) the same way it earlier gained `extraMenus` for the Instructions dropdown (§6.7/gap #7) — `framework/workspace/Workspace.tsx` itself needed zero changes either time. The pattern: when a real second/third consumer needs a shape the shared component can't express, extend the *wrapper's* prop surface, not the shared component, if the shared component's own contract shouldn't have to grow to cover a one-off case.
- **A nested `react-resizable-panels` `Panel` containing its own differently-oriented `Group` just works** — no library workaround needed to mix a horizontal split and a vertical split in the same layout (Factory's Browse | (Viewport+Inspector / Gantt) shape, §6.11). Confirmed live, including independent `collapsible`/`collapsedSize={0}` behavior on the new nested divider.
